<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
  <mapper
  	namespace="com.demo.tenco.model.dao.BoardDAO"> 
  	<insert id="insert" 
		parameterType="com.demo.tenco.model.dto.BoardDTO">
		INSERT INTO
		board(title, content, userId, createDate) 
		VALUES( #{title}, #{content}, 1,NOW()	); 
   	</insert>
    
  </mapper>